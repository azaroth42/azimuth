fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-socketio>=5.10.0
# The socket.io *client* (client.py, tui_client.py) needs websocket-client to
# upgrade off HTTP long-polling.  Without it python-socketio warns
# "only polling transport is available" and every connected player generates a
# continuous stream of GET/POST /socket.io/ requests (~18/min while idle).
websocket-client>=1.7.0
jinja2>=3.1.0
python-dotenv>=1.0.0
python-multipart>=0.0.6
requests
werkzeug>=2.0.0
rich>=10.0.0
textual>=8.2.0
prompt_toolkit>=3.0.0
fastapi_mcp
# fastapi-mcp 0.4.x calls mcp's Server() with a positional version arg,
# which was removed in mcp 2.x -- pin to 1.x until fastapi-mcp is updated
mcp>=1.12.0,<2
